"use client";import {FormEvent,useState} from 'react';import {signIn} from 'next-auth/react';import Link from 'next/link';import {BrainCircuit,ArrowRight,ShieldCheck} from 'lucide-react';import {Button} from '@/components/ui';export default function Login(){const [email,setEmail]=useState('admin@loop.demo'),[password,setPassword]=useState('LoopDemo@2026'),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await signIn('credentials',{email,password,redirect:false});if(r?.error)setError('Invalid email or password.');else location.href='/dashboard';setLoading(false)}return <div className="grid min-h-screen lg:grid-cols-2"><section className="hidden bg-[#17152b] p-12 text-white lg:flex lg:flex-col lg:justify-between"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-white/10"><BrainCircuit/></div><b className="text-xl">LOOP</b></div><div><div className="mb-4 inline-flex rounded-full bg-white/10 px-3 py-1 text-xs">AI CUSTOMER FEEDBACK INTELLIGENCE</div><h1 className="max-w-xl text-5xl font-black leading-tight">Close the loop between what customers say and what teams do.</h1><p className="mt-5 max-w-lg text-white/55">Centralize feedback, discover themes, understand sentiment and ask grounded questions with AI.</p></div><div className="flex items-center gap-2 text-xs text-white/50"><ShieldCheck size={15}/>Tenant-isolated by design</div></section><section className="grid place-items-center p-6"><div className="w-full max-w-md"><div className="mb-8 lg:hidden"><b className="text-2xl">LOOP</b></div><h2 className="text-3xl font-black">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Sign in to your feedback workspace.</p><form onSubmit={submit} className="mt-8 space-y-4"><label className="block text-sm font-semibold">Email<input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border bg-white px-4 py-3 outline-none focus:border-violet-400"/></label><label className="block text-sm font-semibold">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 w-full rounded-xl border bg-white px-4 py-3 outline-none focus:border-violet-400"/></label>{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<Button loading={loading} className="w-full bg-[#17152b] text-white hover:bg-violet-700">Sign in <ArrowRight size={16}/></Button></form><div className="mt-6 rounded-2xl bg-violet-50 p-4 text-xs text-violet-800"><b>Demo access</b><div className="mt-1">admin@loop.demo / LoopDemo@2026</div><div>Analyst: analyst@loop.demo · Viewer: viewer@loop.demo</div></div><p className="mt-6 text-center text-sm text-slate-500">New company? <Link className="font-bold text-violet-700" href="/signup">Create a workspace</Link></p></div></section></div>}
