import {NextResponse} from 'next/server';import {Role} from '@prisma/client';import {requireUser} from './auth';export async function apiUser(){try{return await requireUser()}catch{return null}}export function forbidden(){return NextResponse.json({error:'Forbidden'},{status:403})}export function unauthorized(){return NextResponse.json({error:'Unauthorized'},{status:401})}export function assertRole(role:Role,allowed:Role[]){return allowed.includes(role)}
