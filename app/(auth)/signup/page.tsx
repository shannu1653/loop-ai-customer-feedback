"use client";import {FormEvent,useState} from 'react';import Link from 'next/link';import {useRouter} from 'next/navigation';import {Button} from '@/components/ui';export default function Signup(){const r=useRouter();const [form,setForm]=useState({name:'',email:'',password:'',workspace:''}),[error,setError]=useState(''),[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const res=await fetch('/api/auth/signup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(form)});const data=await res.json();if(!res.ok){setError(data.error||'Could not create account');setLoading(false);return}r.push('/login');}return <div className="grid min-h-screen place-items-center bg-[#f7f6fb] p-6"><div className="w-full max-w-lg rounded-3xl border bg-white p-8 shadow-soft"><div className="text-sm font-black tracking-widest text-violet-600">LOOP</div><h1 className="mt-4 text-3xl font-black">Create your workspace</h1><p className="mt-2 text-sm text-slate-500">Your account becomes the workspace admin.</p><form onSubmit={submit} className="mt-7 space-y-4">{[['name','Your name','text'],['email','Work email','email'],['workspace','Workspace name','text'],['password','Password','password']].map(([k,l,t])=><label key={k} className="block text-sm font-semibold">{l}<input required type={t} value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-2 w-full rounded-xl border bg-white px-4 py-3 outline-none focus:border-violet-400"/></label>)}{error&&<p className="rounded-xl bg-rose-50 p-3 text-sm text-rose-700">{error}</p>}<Button loading={loading} className="w-full bg-[#17152b] text-white">Create workspace</Button></form><p className="mt-6 text-center text-sm text-slate-500">Already have access? <Link href="/login" className="font-bold text-violet-700">Sign in</Link></p></div></div>}
