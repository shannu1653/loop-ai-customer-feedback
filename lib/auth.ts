import {getServerSession,NextAuthOptions} from 'next-auth';import CredentialsProvider from 'next-auth/providers/credentials';import bcrypt from 'bcryptjs';import {db} from './db';import {Role} from '@prisma/client';
export const authOptions:NextAuthOptions={session:{strategy:'jwt'},providers:[CredentialsProvider({name:'Credentials',credentials:{email:{label:'Email',type:'email'},password:{label:'Password',type:'password'}},async authorize(c){if(!c?.email||!c.password)return null;const u=await db.user.findUnique({where:{email:c.email.toLowerCase()},include:{workspace:true}});if(!u||!(await bcrypt.compare(c.password,u.passwordHash)))return null;return {id:u.id,name:u.name,email:u.email,role:u.role,workspaceId:u.workspaceId,workspaceName:u.workspace.name}}})],callbacks:{async jwt({token,user}){if(user){token.role=(user as any).role;token.workspaceId=(user as any).workspaceId;token.workspaceName=(user as any).workspaceName}return token},async session({session,token}){if(session.user){(session.user as any).id=token.sub;(session.user as any).role=token.role;(session.user as any).workspaceId=token.workspaceId;(session.user as any).workspaceName=token.workspaceName}return session}}};
export async function getCurrentUser(){const session=await getServerSession(authOptions);if(!session?.user)return null;return session.user as any}
export async function requireUser(){const u=await getCurrentUser();if(!u)throw new Error('UNAUTHORIZED');return u}
export function canWrite(role:Role){return role===Role.ADMIN||role===Role.ANALYST}
export function canAdmin(role:Role){return role===Role.ADMIN}
